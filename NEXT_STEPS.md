# 📋 Следующие шаги по безопасности

## ✅ Что уже сделано:

1. ✅ Pre-commit hook установлен и работает
2. ✅ Скрипты проверки и удаления секретов созданы
3. ✅ Документация по безопасности добавлена
4. ✅ git-filter-repo установлен

## 🚨 Что нужно сделать СРОЧНО:

### Шаг 1: Сменить все ключи

**КРИТИЧЕСКИ ВАЖНО:** Сначала смените все ключи, иначе удаление из истории не поможет!

#### 1.1. BOT_TOKEN
```bash
# 1. Откройте @BotFather в Telegram
# 2. Отправьте /revoke и выберите вашего бота
# 3. Получите новый токен командой /token
# 4. Обновите BOT_TOKEN в .env файле
```

#### 1.2. DB_PASSWORD
```bash
# Подключитесь к PostgreSQL
psql -U bot_user -d shift_bot

# Измените пароль
ALTER USER bot_user WITH PASSWORD 'новый_надежный_пароль';

# Обновите DB_PASSWORD в .env файле
```

#### 1.3. REDIS_PASSWORD
```bash
# 1. Отредактируйте redis.conf (или docker-compose.yml)
# 2. Измените requirepass на новый пароль
# 3. Перезапустите Redis: docker-compose restart redis
# 4. Обновите REDIS_PASSWORD в .env файле
```

#### 1.4. ENCRYPTION_KEY
```bash
# Сгенерируйте новый ключ
python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# Обновите ENCRYPTION_KEY в .env файле
```

### Шаг 2: Удалить секреты из истории Git

⚠️ **ВНИМАНИЕ:** Это изменит историю репозитория!

```bash
# Запустите скрипт удаления
bash scripts/remove_secrets_from_history.sh
```

Скрипт:
- Заменит реальные ключи на `<REMOVED>` в истории
- Потребует подтверждения
- Автоматически установит git-filter-repo если нужно

### Шаг 3: Force push

⚠️ **ВНИМАНИЕ:** Это перезапишет историю на удаленном репозитории!

```bash
# После удаления секретов из истории
git push --force --all
git push --force --tags
```

### Шаг 4: Уведомить коллабораторов

Если у вас есть коллабораторы, они должны:
1. Удалить старый репозиторий
2. Клонировать заново: `git clone <repository-url>`

## 🔍 Проверка после выполнения

```bash
# Проверить, что .env не отслеживается
git ls-files | grep -E "\.env$"

# Проверить историю на наличие секретов (должно быть пусто или только <REMOVED>)
git log --all --source --full-history -p | grep -E "BOT_TOKEN|DB_PASSWORD|REDIS_PASSWORD|ENCRYPTION_KEY" | grep -v "<REMOVED>"
```

## 📚 Дополнительная информация

- Подробные инструкции: `scripts/README_SECURITY.md`
- Документация по безопасности: `docs/SECURITY.md`
- Скрипт подготовки: `scripts/prepare_secrets_removal.sh`

