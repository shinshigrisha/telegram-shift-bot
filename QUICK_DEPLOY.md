# 🚀 Быстрый старт: Перенос на сервер

## Краткая инструкция для новичков

### 📋 Что нужно перед началом

1. **SSH доступ к серверу** (IP: <IP_АДРЕС_СЕРВЕРА>)
2. **Пароли от баз данных** (PostgreSQL и Redis) из вашего `.env` файла
3. **Токен бота** из `.env` файла

### 🎯 Быстрый перенос (3 шага)

#### Шаг 1: На локальной машине

```bash
# Перейдите в директорию проекта
cd /Users/senya.miroshnichenko/Desktop/telegram-shift-bot

# Запустите скрипт переноса
./scripts/deploy_to_server.sh
```

Скрипт автоматически:
- ✅ Создаст свежие бэкапы баз данных
- ✅ Скопирует все файлы на сервер
- ✅ Покажет дальнейшие инструкции

#### Шаг 2: На сервере (подключитесь через SSH)

```bash
# Подключитесь к серверу
ssh root@<IP_АДРЕС_СЕРВЕРА>

# Перейдите в директорию проекта
cd /opt/telegram-shift-bot

# Создайте файл .env (скопируйте содержимое с локального .env)
nano .env
# Нажмите Ctrl+O для сохранения, Enter для подтверждения, Ctrl+X для выхода

# Запустите скрипт настройки
bash scripts/setup_server.sh
```

Скрипт автоматически:
- ✅ Установит зависимости
- ✅ Запустит PostgreSQL и Redis
- ✅ Предложит восстановить базы данных
- ✅ Настроит автозапуск

#### Шаг 3: Восстановление баз данных

Когда скрипт спросит о восстановлении:

1. **Для PostgreSQL:** Нажмите `y` и выберите последний бэкап
2. **Для Redis:** Нажмите `n` (обычно не требуется)

### ✅ Проверка работы

```bash
# Проверьте статус бота
sudo systemctl status telegram-shift-bot

# Посмотрите логи
sudo journalctl -u telegram-shift-bot -f
```

### 🎉 Готово!

Бот должен работать на сервере. Проверьте в Telegram:
- Отправьте `/start` боту
- Проверьте `/admin` (если вы админ)

---

## 📖 Подробная инструкция

Если что-то пошло не так, смотрите полную инструкцию в файле **DEPLOYMENT_GUIDE.md**

## ⚠️ Важные моменты

1. **Пароли в .env:** Используйте те же пароли, что и на локальной машине, если хотите восстановить старые бэкапы
2. **Токен бота:** Должен быть тот же самый
3. **Автозапуск:** Бот будет автоматически запускаться при перезагрузке сервера

## 🔧 Если что-то не работает

1. Проверьте логи: `sudo journalctl -u telegram-shift-bot -f`
2. Проверьте контейнеры: `docker ps`
3. Проверьте .env файл: `cat /opt/telegram-shift-bot/.env`
4. Смотрите раздел "Решение проблем" в DEPLOYMENT_GUIDE.md

