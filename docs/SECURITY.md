# 🔐 Безопасность

## Хранение секретов

**КРИТИЧЕСКИ ВАЖНО:** Никогда не коммитьте секреты (токены, пароли, ключи) в репозиторий!

### Правила работы с секретами:

1. **Все секреты хранятся в `.env` файле**
   - Файл `.env` добавлен в `.gitignore` и не отслеживается git
   - Используйте `.env.example` как шаблон (без реальных значений)

2. **Pre-commit hook**
   - Автоматически проверяет наличие секретов перед коммитом
   - Если обнаружены секреты - коммит будет отклонен

3. **Если секрет был случайно закоммичен:**
   ```bash
   # 1. Немедленно смените все ключи!
   # 2. Удалите секрет из истории (см. scripts/remove_secrets_from_history.sh)
   # 3. Сделайте force push (только если уверены!)
   ```

## Переменные окружения

### Обязательные переменные:

- `BOT_TOKEN` - токен Telegram бота (получить у @BotFather)
- `DB_PASSWORD` - пароль для PostgreSQL
- `REDIS_PASSWORD` - пароль для Redis
- `ENCRYPTION_KEY` - ключ шифрования (сгенерировать: `python -c "import secrets; print(secrets.token_urlsafe(32))"`)

### Рекомендации по безопасности:

1. **Используйте надежные пароли:**
   - Минимум 16 символов
   - Комбинация букв, цифр и специальных символов
   - Уникальные пароли для каждого сервиса

2. **Регулярно меняйте ключи:**
   - Особенно если подозреваете компрометацию
   - После удаления секретов из истории git

3. **Не храните секреты в:**
   - Коде приложения
   - Комментариях
   - Логах
   - Скриншотах
   - Сообщениях в чатах

4. **Используйте секреты только в:**
   - `.env` файле (локально)
   - Переменных окружения (на сервере)
   - GitHub Secrets (для CI/CD)

## Проверка секретов перед коммитом

Pre-commit hook автоматически проверяет:
- Наличие `.env` файлов в staged изменениях
- Паттерны секретов (токены, пароли, ключи)

Если обнаружены секреты - коммит будет отклонен с сообщением об ошибке.

## Удаление секретов из истории

⚠️ **ВНИМАНИЕ:** Это изменит историю репозитория!

Если секреты были случайно закоммичены:

1. **Немедленно смените все ключи!**
2. Используйте скрипт `scripts/remove_secrets_from_history.sh`
3. Сделайте force push: `git push --force --all`
4. Уведомите всех коллабораторов

## GitHub Secrets (для CI/CD)

Если используете GitHub Actions, храните секреты в:
- Settings → Secrets and variables → Actions → New repository secret

Никогда не хардкодьте секреты в `.github/workflows/*.yml` файлах!

## Проверка безопасности

Перед каждым коммитом убедитесь:
- [ ] `.env` файл не добавлен в git
- [ ] Нет секретов в коде
- [ ] Нет секретов в комментариях
- [ ] Pre-commit hook работает корректно

## Дополнительные ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [GitHub Security Best Practices](https://docs.github.com/en/code-security)
- [12 Factor App - Config](https://12factor.net/config)

