# Как узнать и изменить ID для тестирования

## 1. Как узнать Chat ID группы

### Способ 1: Через бота @RawDataBot
1. Добавьте бота @RawDataBot в вашу группу
2. Отправьте любое сообщение в группе
3. Бот покажет `chat.id` - это и есть Chat ID группы
4. Обычно для групп начинается с `-100`

### Способ 2: Через бота @userinfobot
1. Добавьте бота @userinfobot в группу
2. Бот покажет информацию о группе, включая Chat ID

### Способ 3: Через команду бота
1. Добавьте бота в группу
2. Используйте команду `/get_topic_id` в группе
3. Бот покажет информацию о группе

## 2. Как узнать Topic ID

### Способ 1: Через команду `/get_topic_id`
1. Перейдите в нужную тему форум-группы
2. Выполните команду `/get_topic_id`
3. Бот покажет `message_thread_id` - это и есть Topic ID

### Способ 2: Через бота @RawDataBot
1. Добавьте бота @RawDataBot в группу
2. Перейдите в нужную тему
3. Отправьте любое сообщение в теме
4. Бот покажет `message_thread_id` - это и есть Topic ID

### Способ 3: Через админ-панель
1. Откройте `/admin` → "📌 Установить тему"
2. Выберите тип темы
3. **Важно:** Выполните выбор темы прямо в нужной теме форум-группы
4. Бот автоматически определит Topic ID

## 3. Как узнать ваш Telegram ID

### Способ 1: Через бота @userinfobot
1. Начните диалог с ботом @userinfobot
2. Бот покажет ваш User ID

### Способ 2: Через бота @RawDataBot
1. Начните диалог с ботом @RawDataBot
2. Отправьте любое сообщение
3. Бот покажет `from.id` - это и есть ваш User ID

## 4. Как изменить ID в тестовом скрипте

Откройте файл `scripts/comprehensive_test.py` и найдите строки:

```python
# Тестовые данные
TEST_GROUP_NAME = "ТЕСТ-ГРУППА"
TEST_CHAT_ID = -1000000000000  # ⬅️ ИЗМЕНИТЕ: Chat ID вашей тестовой группы
TEST_TOPIC_ID = 123  # ⬅️ ИЗМЕНИТЕ: Topic ID темы "отметки на слот"
TEST_ADMIN_ID = settings.ADMIN_IDS[0] if settings.ADMIN_IDS else 445137184  # ⬅️ ИЗМЕНИТЕ: Ваш Telegram ID
```

### Пример изменения:

```python
# Тестовые данные
TEST_GROUP_NAME = "Моя тестовая группа"
TEST_CHAT_ID = -1001234567890  # Реальный Chat ID вашей группы
TEST_TOPIC_ID = 456  # Реальный Topic ID темы
TEST_ADMIN_ID = 123456789  # Ваш реальный Telegram ID
```

## 5. Важные замечания

⚠️ **Перед запуском тестов убедитесь:**
- Бот добавлен в группу с указанным Chat ID
- У бота есть права администратора в группе
- Тема с указанным Topic ID существует в группе
- Бот имеет доступ к теме

## 6. Быстрая проверка ID

Создайте простой скрипт для проверки:

```python
# test_ids.py
import asyncio
from aiogram import Bot
from config.settings import settings

async def check_ids():
    bot = Bot(token=settings.BOT_TOKEN)
    
    # Получите Chat ID, отправив сообщение в группу
    # и проверив через @RawDataBot
    
    # Получите Topic ID, выполнив /get_topic_id в теме
    
    await bot.session.close()

asyncio.run(check_ids())
```

## 7. Альтернативный способ: Использование админ-панели

Вместо ручного изменения ID, вы можете:

1. Создать группу через админ-панель: `/admin` → "➕ Создать группу для ЗИЗ"
2. Настроить темы через админ-панель: `/admin` → "📌 Установить тему"
3. Затем использовать реальные данные из базы данных

Это проще, так как бот автоматически определит все ID из контекста сообщений.

