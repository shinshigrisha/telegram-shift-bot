# Telegram Bot для планирования смен

## 📖 Описание

Telegram-бот для автоматического создания и управления опросами о планировании смен в рабочих группах. Бот автоматизирует процесс записи сотрудников на рабочие смены, создает ежедневные опросы, собирает голоса, формирует отчеты и отправляет результаты.

### Основные возможности

- 🤖 **Автоматическое создание опросов** каждый день в 09:00
- ⏰ **Автоматическое закрытие опросов** в 19:00
- 🔔 **Система напоминаний** о закрытии записи
- 👥 **Идентификация курьеров** по тегам в именах Telegram (8958, 7368, 6028)
- 📋 **Работа с темами форум-групп** для организации опросов
- 📢 **Рассылка сообщений** по всем группам
- 👑 **Админ-панель** для удобного управления

### Архитектура проекта

Проект использует **Layered Architecture** с четким разделением ответственности:

```
src/
├── handlers/      # Обработчики событий (UI слой)
│   ├── admin_panel.py              # Главный файл админ-панели (точка входа)
│   ├── admin_panel_navigation.py   # Навигация и главное меню
│   ├── admin_panel_monitoring.py   # Мониторинг системы
│   ├── admin_panel_groups.py       # Управление группами
│   ├── admin_panel_slots.py        # Настройка слотов
│   ├── admin_panel_schedule.py     # Настройка расписания
│   ├── admin_panel_topics.py       # Настройка тем
│   ├── admin_panel_polls.py        # Управление опросами
│   ├── admin_panel_verification.py # Верификация пользователей
│   ├── admin_panel_vote_restore.py # Восстановление голосов
│   ├── admin_panel_users.py        # Управление пользователями
│   ├── admin_panel_broadcast.py    # Рассылка сообщений
│   └── ...                         # Другие обработчики
├── services/      # Бизнес-логика
├── repositories/  # Доступ к данным (Data Access Layer)
├── models/        # SQLAlchemy модели (ORM)
├── middlewares/   # Middleware для обработки запросов
├── utils/         # Вспомогательные утилиты
│   ├── admin_keyboards.py    # Утилиты для создания клавиатур админ-панели
│   ├── group_formatters.py   # Утилиты для форматирования названий групп
│   └── ...                   # Другие утилиты
└── states/        # FSM состояния для диалогов
```

**Принципы архитектуры:**
- **Dependency Injection**: Все зависимости передаются через параметры
- **Repository Pattern**: Доступ к БД только через репозитории
- **Service Layer**: Бизнес-логика в сервисах, handlers только координируют
- **Middleware Pattern**: Cross-cutting concerns через middleware

## 🔧 Технологии

### Основной стек

- **Python 3.11+** — язык программирования
- **Aiogram 3.x** — асинхронный фреймворк для Telegram Bot API
- **PostgreSQL** — реляционная база данных для хранения групп, опросов, пользователей, голосов
- **Redis** — хранилище состояний FSM (Finite State Machine)
- **SQLAlchemy 2.0** — ORM для работы с базой данных
- **APScheduler** — планирование автоматических задач
- **Pydantic** — валидация настроек и данных

### Дополнительные библиотеки

- **asyncpg** — асинхронный драйвер PostgreSQL
- **python-dotenv** — загрузка переменных окружения
- **pytest** — тестирование (pytest-asyncio для async тестов)

### Инфраструктура

- **Docker & Docker Compose** — контейнеризация PostgreSQL и Redis
- **APScheduler** — автоматическое выполнение задач по расписанию

## 🚀 Подключение и установка

### Требования

- Python 3.11 или выше
- Docker и Docker Compose (для БД и Redis)
- Токен Telegram бота от [@BotFather](https://t.me/BotFather)

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd telegram-shift-bot
```

### Шаг 2: Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

⚠️ **ВАЖНО:** Никогда не коммитьте файл `.env` с реальными ключами в репозиторий!

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите необходимые параметры:

```env
# Telegram Bot
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321  # ID админов через запятую

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=shift_bot
DB_USER=bot_user
DB_PASSWORD=your_db_password_here

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here
REDIS_DB=0

# Schedule (время создания и закрытия опросов)
POLL_CREATION_HOUR=9
POLL_CREATION_MINUTE=0
POLL_CLOSING_HOUR=19
POLL_CLOSING_MINUTE=0

# Security
ENCRYPTION_KEY=your_encryption_key_here

# Управление уведомлениями (True/False)
ENABLE_ADMIN_NOTIFICATIONS=True
ENABLE_GROUP_REMINDERS=True
ENABLE_COURIER_WARNINGS=True    
ENABLE_POLL_CREATION_NOTIFICATIONS=True
ENABLE_HEALTH_CHECK_NOTIFICATIONS=True
ENABLE_VERIFICATION=False  # Верификация пользователей (по умолчанию отключена)
```

### Шаг 5: Запуск базы данных

```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL на порту 5432
- Redis на порту 6379

### Шаг 6: Инициализация базы данных

```bash
python scripts/first_setup.py
```

Это создаст все необходимые таблицы в базе данных.

### Шаг 7: Запуск бота

```bash
python src/main.py
```

Бот должен запуститься и начать работать. Проверьте логи в `logs/bot.log` на наличие ошибок.

## 📦 Деплой

### 📖 Подробная инструкция

**📘 [Полное руководство по деплою](docs/DEPLOYMENT_GUIDE.md)** — подробная инструкция по развертыванию бота на сервере со всеми шагами, примерами команд и решением проблем.

### Быстрый старт

Для переноса бота на сервер с сохранением всех данных используйте готовые скрипты:

**На локальной машине:**
1. Настройте параметры в `scripts/deploy_to_server.sh` (IP сервера, пользователь, путь)
2. Запустите: `./scripts/deploy_to_server.sh`

**На сервере:**
1. Подключитесь по SSH: `ssh root@<IP_АДРЕС_СЕРВЕРА>`
2. Перейдите в директорию: `cd /opt/telegram-shift-bot`
3. Создайте `.env` файл с настройками
4. Запустите настройку: `bash scripts/setup_server.sh`

Скрипты автоматически:
- ✅ Создают бэкапы баз данных (PostgreSQL и Redis)
- ✅ Копируют файлы на сервер
- ✅ Настраивают окружение
- ✅ Восстанавливают базы данных
- ✅ Настраивают автозапуск через systemd

**📝 Шпаргалка:** См. также [scripts/DEPLOY_COMMANDS.md](scripts/DEPLOY_COMMANDS.md) для быстрого доступа к командам.

### Локальный запуск (для разработки)

См. раздел [Подключение и установка](#-подключение-и-установка)

### Docker (рекомендуется для production)

#### Сборка образа

```bash
docker build -t telegram-shift-bot .
```

#### Запуск с Docker Compose

Создайте `docker-compose.prod.yml`:

```yaml
version: '3.8'

services:
  bot:
    build: .
    container_name: shift-bot
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
    volumes:
      - ./logs:/app/logs
      - ./reports:/app/reports
      - ./backups:/app/backups

  postgres:
    image: postgres:15-alpine
    container_name: shift-bot-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: shift_bot
      POSTGRES_USER: bot_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bot_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: shift-bot-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
```

Запуск:

```bash
docker-compose -f docker-compose.prod.yml up -d
```

### Systemd (для Linux серверов)

Создайте файл `/etc/systemd/system/telegram-shift-bot.service`:

```ini
[Unit]
Description=Telegram Shift Bot
After=network.target postgresql.service redis.service

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/telegram-shift-bot
Environment="PATH=/path/to/telegram-shift-bot/venv/bin"
ExecStart=/path/to/telegram-shift-bot/venv/bin/python src/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация и запуск:

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-shift-bot
sudo systemctl start telegram-shift-bot
sudo systemctl status telegram-shift-bot
```

### Остановка бота

Бот корректно обрабатывает сигналы остановки (`SIGINT`, `SIGTERM`) и завершает работу с сохранением всех данных.

**При локальном запуске:**
```bash
# Нажмите Ctrl+C в терминале
^C
```

**При запуске в Docker:**
```bash
docker stop shift-bot
# или
docker-compose stop
```

**При запуске через systemd:**
```bash
sudo systemctl stop telegram-shift-bot
```

⚠️ **Важно:** Не используйте `kill -9` без крайней необходимости — это может привести к потере данных.

## 📋 Функционал

### Для пользователей

#### Команды

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом. Показывает приветственное сообщение и список доступных команд |
| `/help` | Показать подробную справку по всем командам с примерами использования |
| `/my_votes` | Просмотр своих голосов в опросах (в разработке) |
| `/schedule` | Просмотр расписания смен (в разработке) |

#### Верификация

При первом использовании бота (если `ENABLE_VERIFICATION=True`) пользователь автоматически получает запрос на ввод имени и фамилии. Эти данные сохраняются и используются для отображения в результатах опросов.

#### Участие в опросах

Пользователи просто голосуют в опросах, которые бот отправляет в группы каждый день в 09:00. Опросы доступны до 19:00, после чего автоматически закрываются.

### Для администраторов

#### Админ-панель

Команда `/admin` открывает админ-панель с кнопками для быстрого доступа ко всем функциям.

#### Команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `/admin` | Открыть админ-панель | `/admin` |
| `/add_group` | Добавить новую группу | `/add_group ЗИЗ-1 -1001234567890 [123]` |
| `/setup_ziz` | Настроить слоты времени для группы | `/setup_ziz ЗИЗ-1` |
| `/list_groups` | Показать список всех групп | `/list_groups` |
| `/set_topic` | Установить topic_id для темы "отметки на слот" | `/set_topic ЗИЗ-1 [123]` |
| `/set_arrival_topic` | Установить topic_id для темы "приход/уход" | `/set_arrival_topic ЗИЗ-1 123` |
| `/set_general_topic` | Установить topic_id для темы "общий чат" | `/set_general_topic ЗИЗ-1 123` |
| `/set_important_topic` | Установить topic_id для темы "важная информация" | `/set_important_topic ЗИЗ-1 123` |
| `/get_topic_id` | Показать topic_id из контекста текущего сообщения | `/get_topic_id` |
| `/create_polls` | Создать опросы вручную для всех активных групп | `/create_polls` |
| `/get_report` | Получить отчет по группе за указанную дату | `/get_report ЗИЗ-1 17.12.2025` |
| `/stats` | Показать статистику системы | `/stats` |
| `/status` | Показать статус системы (CPU, RAM, Disk) | `/status` |
| `/logs` | Показать последние 20 строк логов | `/logs` |

### Автоматическое расписание

Бот автоматически выполняет следующие действия:

#### 09:00 — Создание опросов на следующий день

1. Создание опросов во всех активных группах в теме "отметки на слот"
2. Закрепление сообщений с опросами (без уведомления)
3. Создание слотов в базе данных для каждого опроса
4. Уведомления админам о количестве созданных опросов и ошибках

**Повторная попытка:** Если создание опроса не удалось, бот повторяет попытку через 5 минут для неудачных групп.

#### 18:00 — Напоминание о закрытии опроса

Отправляет напоминание "Остался один час до конца опроса!" в тему "общий чат" для всех активных групп с опросом на завтра.

**Условие:** Выполняется только если `ENABLE_GROUP_REMINDERS=True` в `.env`.

#### 19:00 — Закрытие опросов (основная задача)

1. Автоматическое закрытие всех активных опросов текущего дня через Telegram API
2. Обновление статуса в БД на "closed"

#### 19:05, 19:10, 19:15, ..., 19:55 — Периодические проверки закрытия

Повторяет логику закрытия опросов **каждые 5 минут** для гарантии закрытия даже при сбоях.

#### 20:00 — Финальная проверка закрытия

Последняя попытка закрыть опросы на случай, если что-то было пропущено.

#### Каждые 30 минут (XX:30) — Health Check

Проверяет:
- Наличие опросов на завтра для всех групп
- Статус опросов (должны быть "active")
- Доступность бота в группах

Отправляет уведомления админам о проблемах.

**Условие:** Выполняется только если `ENABLE_HEALTH_CHECK_NOTIFICATIONS=True` в `.env`.

#### При старте бота — Проверка просроченных опросов

Если бот запущен после 19:00, проверяет и закрывает просроченные опросы через 5 секунд после старта.

### Настройка времени

Время создания и закрытия опросов можно настроить:

1. **Через админ-панель** (рекомендуется):
   - `/admin` → "⏰ Настройка расписания" → "✏️ Редактировать"
   - Пошагово введите время создания опросов и закрытия опросов
   - Изменения автоматически сохраняются в `.env` файл

2. **Вручную в файле `.env`**:
   - `POLL_CREATION_HOUR` и `POLL_CREATION_MINUTE` (по умолчанию 9:00)
   - `POLL_CLOSING_HOUR` и `POLL_CLOSING_MINUTE` (по умолчанию 19:00)

⚠️ **Важно:** Изменения вступят в силу после перезапуска бота.

### Формат слотов времени

#### Формат ввода слотов

При настройке группы через `/setup_ziz` или админ-панель слоты вводятся в следующем формате:

```
время_начала-время_конца:лимит
```

**Примеры:**
- `07:30-19:30:3` - слот с 07:30 до 19:30, лимит 3 человека
- `08:00-20:00:2` - слот с 08:00 до 20:00, лимит 2 человека
- `10:00-22:00:1` - слот с 10:00 до 22:00, лимит 1 человек

Можно вводить несколько слотов сразу (каждый с новой строки) или по одному. Для завершения настройки отправьте `готово`.

#### Формат отображения в опросах

Опции в опросах отображаются в следующем формате:
- Лимит 1: `10:00-22:00 - 1 человек`
- Лимит 2: `10:00-22:00 - 1 человек, 10:00-22:00 - 2 человека`
- Лимит 3+: `10:00-22:00 - 1 человек, 10:00-22:00 - 2 человека, - 3 человека`

### Работа с темами форум-групп

Бот поддерживает работу с темами форум-групп Telegram для организации опросов и уведомлений.

#### Типы тем

1. **"отметки на слот"** (`telegram_topic_id`)
   - Сюда создаются опросы каждый день в 09:00
   - Опросы создаются в существующей теме (тема не создается автоматически)

2. **"приход/уход"** (`arrival_departure_topic_id`)
   - Используется для других целей

3. **"общий чат"** (`general_chat_topic_id`)
   - Сюда отправляются напоминания о закрытии записи

4. **"важная информация"** (`important_info_topic_id`)
   - Сюда можно отправлять важные информационные сообщения
   - Используется для рассылки через админ-панель

#### Как установить topic_id

**Через админ-панель (рекомендуется):**
1. Откройте `/admin` → "📌 Установить тему"
2. Выберите тип темы (Отметки на слот, Приход/уход, Общий чат, Важная информация)
3. **Важно:** Выполните выбор темы прямо в нужной теме форум-группы
4. Выберите группу из списка
5. Topic_id определится автоматически и будет установлен

**Через команды:**
- Выполните команду `/get_topic_id` в нужной теме форум-группы
- При создании группы выполните `/add_group название chat_id` прямо в теме, topic_id определится автоматически
- При установке темы выполните `/set_topic название_группы` в теме, topic_id определится автоматически
- Вручную: перешлите сообщение из темы боту @RawDataBot, он покажет `message_thread_id`

### Идентификация курьеров по тегам

Бот автоматически идентифицирует курьеров ДС 8958 по тегам в их именах Telegram:
- **8958** - основной тег курьеров ДС 8958
- **7368** - дополнительный тег
- **6028** - дополнительный тег

#### Как это работает

1. **Автоматическая идентификация**: При проверке участников группы бот ищет теги в display names пользователей Telegram
2. **Автоматическое добавление в БД**: Курьеры с тегами автоматически добавляются в базу данных при первом голосовании
3. **Извлечение имени**: Имя и фамилия извлекаются из display name (удаляются цифры и теги)

**Примеры идентификации:**
- `"Иванов Иван 8958"` → Имя: "Иванов", Фамилия: "Иван"
- `"Петров Петр 7368"` → Имя: "Петров", Фамилия: "Петр"
- `"Сидоров Сидор 6028"` → Имя: "Сидоров", Фамилия: "Сидор"

## 👑 Использование админ-панели

### 📖 Подробная инструкция

**📘 [Полная инструкция по использованию админ-панели](docs/ADMIN_PANEL_GUIDE.md)** — подробное руководство для администраторов со всеми функциями, пошаговыми инструкциями и примерами.

### Открытие админ-панели

Выполните команду `/admin` для открытия админ-панели. Доступна только администраторам (ID указаны в `ADMIN_IDS` в `.env`).

### Основные функции админ-панели

#### ➕ Создать группу для ЗИЗ

Пошаговое создание новой группы:
1. Введите название группы (например, "ЗИЗ-1")
2. Введите chat_id группы (например, "-1001234567890")
3. Бот уведомит о необходимости настройки тем

**Совет:** Если выполнить команду в теме форум-группы, topic_id определится автоматически.

#### ⚙️ Настройки слотов

Интерактивная настройка слотов времени для группы через удобный интерфейс:
1. Выберите группу из списка
2. Выберите количество слотов (от 1 до 5)
3. Для каждого слота настройте:
   - **🕐 Начало слота** — выберите час (00-11) и минуты (00 или 30) через inline-клавиатуру
   - **🕐 Конец слота** — выберите час (00-11) и минуты (00 или 30) через inline-клавиатуру
   - **👥 Количество курьеров** — введите число вручную
4. После настройки всех слотов нажмите "✅ Готово"
5. Просмотрите сводку и подтвердите настройки

**Преимущества:**
- Удобный интерфейс с inline-клавиатурой для выбора времени
- Визуальное отображение текущих настроек
- Возможность редактирования каждого параметра отдельно
- Просмотр сводки перед сохранением

**Примеры слотов:**
- Слот 1: 07:30-19:30, лимит 3 человека
- Слот 2: 08:00-20:00, лимит 2 человека
- Слот 3: 10:00-22:00, лимит 1 человек

#### ⏰ Настройка расписания

Просмотр и редактирование автоматического расписания:
1. Бот покажет текущие настройки:
   - Время создания опросов (hh:mm)
   - Время закрытия опросов (hh:mm)
   - Часы напоминаний (через запятую или `0` для отключения)
2. Нажмите "✏️ Редактировать" для изменения
3. Пошагово введите новые значения
4. Изменения автоматически сохраняются в `.env` файл

⚠️ **Важно:** После изменения времени перезапустите бота.

#### 📌 Установить тему

Автоматическая установка topic_id из контекста для 4 типов тем:

1. **📋 Отметки на слот** (`telegram_topic_id`)
2. **📥 Приход/уход** (`arrival_departure_topic_id`)
3. **💬 Общий чат** (`general_chat_topic_id`)
4. **📢 Важная информация** (`important_info_topic_id`)

**Как использовать:**
1. Выберите тип темы
2. **Важно:** Выполните выбор темы прямо в нужной теме форум-группы
3. Выберите группу из списка
4. Topic_id определится автоматически и будет установлен

#### 📝 Создать опросы вручную

Создание опросов для всех активных групп (для тестирования):
- Если опросы уже существуют, они будут отправлены первым сообщением в личку администратора
- Полезно для проверки работы бота перед автоматическим созданием

#### 🔄 Пересоздать опросы

Принудительное создание опросов с закрытием существующих активных опросов:
- Требует подтверждения, так как данные голосования будут потеряны
- Используйте только в крайних случаях

#### 🔍 Проверить слоты

Проверка всех активных групп на:
- Незаполненные слоты (где никто не отметился)
- Неотметившихся курьеров (идентифицируются по тегам)

**Примечание:** Функция проверки доступна через админ-панель, но автоматическая отправка замечаний отключена.

#### 📊 Вывести результат

Просмотр результатов опроса для выбранной группы:
1. Выберите группу из списка
2. Бот покажет результаты в виде текстового отчета
3. Результаты содержат фамилию и имя курьеров, выбранные слоты времени

#### 🔒 Закрыть опрос

Досрочное закрытие опроса для выбранной группы:
1. Выберите группу из списка
2. Опрос будет закрыт через Telegram API
3. После закрытия опрос больше не принимает голоса

**Важно:** После закрытия пользователи не смогут проголосовать в этом опросе.

#### 🔒 Закрыть все опросы

Массовое закрытие всех активных опросов во всех группах:
- Закрывает все активные опросы (не только на сегодня, но и на завтра и другие даты)
- Использует надежный метод закрытия через Telegram API
- Обновляет статус в базе данных на "closed"
- После закрытия опросы больше не принимают голоса

**Как это работает:**
1. Бот получает все активные опросы из базы данных
2. Для каждого опроса вызывается `bot.stop_poll()` — закрывает опрос в Telegram API
3. Статус опроса обновляется на "closed" в базе данных
4. Обработчик голосов проверяет статус и игнорирует голоса для закрытых опросов

**Результат:** После закрытия пользователи не смогут проголосовать в закрытых опросах ни через Telegram интерфейс, ни программно.

#### 🔎 Найти и открыть опросы на завтра

Принудительный поиск опросов на завтра:
1. Синхронизация опросов из Telegram (если опросы есть в группах, но отсутствуют в БД)
2. Создание текстовых отчетов с результатами
3. Все результаты отправляются админу в личку

#### 📢 Рассылка по группам

Отправка сообщения во все группы в выбранную тему:
1. Выберите тип темы (4 типа тем)
2. Введите сообщение для рассылки
3. Сообщение будет отправлено во все активные группы в выбранную тему

**Примечание:** Рассылка работает только для групп, у которых настроена соответствующая тема.

## ⚠️ Важные обновления

### ✅ Рефакторинг админ-панели: разбиение на модули

**Проблема:** Файл `admin_panel.py` содержал 4638 строк кода, что затрудняло поддержку и развитие функциональности.

**Решение:**
- ✅ Файл `admin_panel.py` разбит на 11 логических модулей по функциональности
- ✅ Каждый модуль отвечает за свою область функциональности
- ✅ Основной файл `admin_panel.py` теперь служит точкой входа (43 строки, только импорты)
- ✅ Все модули используют единый router, который объединяет все под-роутеры

**Структура модулей:**
1. `admin_panel_navigation.py` - Навигация и главное меню
2. `admin_panel_monitoring.py` - Мониторинг системы
3. `admin_panel_groups.py` - Управление группами
4. `admin_panel_slots.py` - Настройка слотов
5. `admin_panel_schedule.py` - Настройка расписания
6. `admin_panel_topics.py` - Настройка тем
7. `admin_panel_polls.py` - Управление опросами
8. `admin_panel_verification.py` - Верификация пользователей
9. `admin_panel_vote_restore.py` - Восстановление голосов
10. `admin_panel_users.py` - Управление пользователями
11. `admin_panel_broadcast.py` - Рассылка сообщений

**Результат:** 
- Улучшена читаемость и поддерживаемость кода
- Упрощена навигация по коду
- Каждый модуль можно редактировать независимо
- Сохранена полная обратная совместимость - все функции работают как прежде

### ✅ Добавлен механизм повторной попытки при ошибке "Poll not found"

**Проблема:** При создании опросов в 09:00 возникала race condition: опрос создавался в Telegram, но еще не был закоммичен в БД. Ранние голоса пользователей терялись, так как обработчик не мог найти опрос в БД.

**Решение:**
- ✅ Добавлен механизм retry с 3 попытками и прогрессивными задержками (0.5, 1.0, 2.0 секунды)
- ✅ Перед каждой попыткой сбрасывается кэш сессии БД для получения свежих данных
- ✅ Улучшено логирование с информацией о времени выполнения и количестве попыток
- ✅ При успешном нахождении опроса после retry логируется информация о разрешении race condition

**Результат:** Голоса пользователей больше не теряются из-за race condition при создании опросов. Система автоматически повторяет попытки найти опрос в БД, что решает проблему потери голосов в первые секунды после создания опроса.

### ✅ Улучшена функция закрытия всех опросов

**Изменения:**
- ✅ Функция "Закрыть все опросы" теперь закрывает **все активные опросы** (не только на сегодня)
- ✅ Использует надежный метод `_close_single_poll()` из `PollService` для корректного закрытия
- ✅ После закрытия опросы **полностью блокируют голосование**:
  - Telegram API не позволяет голосовать в закрытых опросах
  - Обработчик голосов дополнительно проверяет статус и игнорирует голоса для закрытых опросов
- ✅ Улучшено сообщение с результатами закрытия

**Результат:** Теперь кнопка "Закрыть все опросы" гарантированно предотвращает дальнейшее голосование во всех закрытых опросах.

### 🔄 Улучшена настройка слотов через админ-панель

**Изменения:**
- ✅ Добавлен интерактивный интерфейс настройки слотов с inline-клавиатурой
- ✅ Выбор времени через удобные кнопки (часы 00-11, минуты 00/30)
- ✅ Пошаговая настройка каждого параметра слота отдельно
- ✅ Визуальное отображение текущих настроек
- ✅ Просмотр сводки перед сохранением

**Преимущества:**
- Не нужно вводить время вручную в формате `07:30-19:30:3`
- Удобный выбор времени через кнопки
- Меньше ошибок при настройке
- Возможность редактирования каждого параметра отдельно

### 🏗️ Рефакторинг кода админ-панели

**Изменения:**
- ✅ Вынесены утилиты клавиатур в отдельный модуль `src/utils/admin_keyboards.py`
- ✅ Вынесены утилиты форматирования в отдельный модуль `src/utils/group_formatters.py`
- ✅ Удалено debug-логирование из кода
- ✅ Улучшена структура кода и его поддерживаемость

**Результат:** Код стал более модульным, легче поддерживается и расширяется.

### ✅ Исправлена критическая проблема с сохранением голосов

**Проблема:** Ранее голоса не сохранялись в базу данных при голосовании из-за отсутствия явного `commit()` в `DatabaseMiddleware`, что приводило к:
- Некорректному обновлению счетчиков слотов
- Потере всех голосов при перезапуске бота

**Решение:**
- ✅ Добавлен явный `commit()` в `DatabaseMiddleware` после успешного выполнения обработчиков
- ✅ Добавлен `rollback()` при ошибках для корректной обработки исключений
- ✅ Теперь при каждом голосовании:
  - Голос автоматически сохраняется в таблицу `user_votes`
  - Слот обновляется (`current_users++`, добавление `user_id` в `user_ids`)
  - Поддерживается смена выбора (удаление из старого слота, добавление в новый)
  - Корректно обрабатывается опция "Выходной"

**Результат:** Все голосования теперь корректно сохраняются в базу данных, текстовые отчеты и проверки курьеров работают правильно!

### 🔄 Улучшена надежность закрытия опросов

Добавлена многоуровневая система проверок:
- Основная задача в 19:00
- Периодические проверки каждые 5 минут с 19:05 до 19:55
- Финальная проверка в 20:00
- Проверка при старте бота (если запущен после 19:00)

Это гарантирует, что опросы будут закрыты даже при сбоях или перезапуске бота.

### 🔍 Идентификация курьеров по тегам

- ✅ Бот автоматически идентифицирует курьеров по тегам (8958, 7368, 6028) в именах Telegram
- ✅ Курьеры с тегами автоматически добавляются в БД при первом голосовании
- ✅ Автоматическое извлечение имен из display names Telegram

### 🔍 Проверка слотов

- ✅ Бот выявляет незаполненные слоты (current_users = 0)
- ✅ Бот выявляет слоты с превышением лимита (current_users > max_users)

**Примечание:** Автоматическая отправка замечаний курьерам отключена.

## 🧪 Тестирование

Проект включает комплексные unit-тесты для проверки функциональности бота.

### Запуск тестов

```bash
# Запуск всех тестов
pytest

# Запуск с подробным выводом
pytest -v

# Запуск конкретного файла тестов
pytest tests/test_user_handlers.py

# Запуск конкретного теста
pytest tests/test_user_handlers.py::test_cmd_help_user

# Запуск с покрытием кода
pytest --cov=src --cov-report=html
```

### Структура тестов

Тесты организованы по модулям:
- `test_user_handlers.py` - пользовательские команды
- `test_verification_handlers.py` - верификация пользователей
- `test_admin_handlers.py` - админские команды
- `test_admin_panel.py` - админ-панель
- `test_poll_handlers.py` - обработка ответов на опросы
- `test_monitoring_handlers.py` - команды мониторинга
- `test_report_handlers.py` - команды отчетов
- `test_slot_validation.py` - валидация формата слотов времени
- `test_group_service.py` - сервис групп
- `test_poll_service.py` - сервис опросов
- `test_user_service.py` - сервис пользователей

## 📁 Структура проекта

```
telegram-shift-bot/
├── src/
│   ├── handlers/          # Обработчики событий (UI слой)
│   │   ├── admin_panel.py              # Главный файл админ-панели (точка входа)
│   │   ├── admin_panel_navigation.py   # Навигация и главное меню
│   │   ├── admin_panel_monitoring.py   # Мониторинг системы
│   │   ├── admin_panel_groups.py       # Управление группами
│   │   ├── admin_panel_slots.py        # Настройка слотов
│   │   ├── admin_panel_schedule.py     # Настройка расписания
│   │   ├── admin_panel_topics.py       # Настройка тем
│   │   ├── admin_panel_polls.py        # Управление опросами
│   │   ├── admin_panel_verification.py # Верификация пользователей
│   │   ├── admin_panel_vote_restore.py # Восстановление голосов
│   │   ├── admin_panel_users.py        # Управление пользователями
│   │   ├── admin_panel_broadcast.py    # Рассылка сообщений
│   │   ├── admin.py                    # Админские команды
│   │   ├── poll_handlers.py            # Обработка ответов на опросы
│   │   └── ...                         # Другие обработчики
│   ├── services/          # Бизнес-логика
│   │   ├── poll_service.py      # Сервис для работы с опросами
│   │   ├── group_service.py     # Сервис для работы с группами
│   │   └── ...                  # Другие сервисы
│   ├── repositories/      # Доступ к данным (Data Access Layer)
│   │   ├── poll_repository.py   # Репозиторий опросов
│   │   ├── group_repository.py  # Репозиторий групп
│   │   └── ...                  # Другие репозитории
│   ├── models/            # SQLAlchemy модели (ORM)
│   ├── middlewares/       # Middleware для обработки запросов
│   ├── states/            # FSM состояния для диалогов
│   ├── utils/             # Вспомогательные утилиты
│   │   ├── admin_keyboards.py   # Утилиты для создания клавиатур админ-панели
│   │   ├── group_formatters.py # Утилиты для форматирования названий групп
│   │   └── ...                  # Другие утилиты
│   ├── bot.py             # Настройка бота
│   └── main.py            # Точка входа
├── config/
│   └── settings.py        # Настройки приложения
├── db/
│   └── init.sql           # SQL-скрипты для инициализации БД
├── scripts/               # Вспомогательные скрипты
├── tests/                 # Тесты
├── reports/               # Отчеты
├── logs/                  # Логи приложения
├── backups/               # Резервные копии
├── docker-compose.yml     # Docker Compose конфигурация
├── Dockerfile             # Docker образ бота
├── requirements.txt       # Зависимости Python
└── README.md              # Документация
```

## 🔧 Дополнительные скрипты

### Скрипты для работы с курьерами

```bash
# Добавить курьеров из опросов в БД (автоматически по тегам)
python scripts/add_couriers_from_list.py

# Сравнить список сотрудников с БД
python scripts/compare_employees_with_db.py [путь_к_файлу.xml]

# Показать всех пользователей в БД
python scripts/list_all_users.py

# Обновить имя пользователя
python scripts/update_user_name.py
```

### Скрипты для синхронизации опросов

```bash
# Синхронизировать опросы из Telegram в БД (на завтра)
python scripts/sync_polls_from_telegram.py

# Синхронизировать опросы на конкретную дату
python scripts/sync_polls_from_telegram.py --date 2025-12-25
```

### Скрипты для проверки

```bash
# Проверить статус опросов во всех группах
python scripts/check_polls_status.py

# Проверить проблемы со слотами (незаполненные, превышение лимита)
python scripts/check_slots_issues.py

# Получить список участников группы через Telegram API
python scripts/get_all_group_members.py <chat_id>

# Вывести результаты всех активных опросов
python scripts/show_polls_results.py

# Проверить голоса по telegram_poll_id из логов
python scripts/check_votes_by_poll_id.py
```

## 📝 Миграции базы данных

После обновления кода необходимо применить миграции:

```bash
# 1. Создание таблицы users (для верификации пользователей)
docker exec shift-bot-postgres psql -U bot_user -d shift_bot -c "CREATE TABLE IF NOT EXISTS users (id BIGINT PRIMARY KEY, first_name VARCHAR(100), last_name VARCHAR(100), username VARCHAR(100), is_verified BOOLEAN DEFAULT FALSE, created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_users_verified ON users(is_verified);"

# 2. Добавление колонок для тем в таблицу groups
docker exec shift-bot-postgres psql -U bot_user -d shift_bot -c "ALTER TABLE groups ADD COLUMN IF NOT EXISTS arrival_departure_topic_id INTEGER; ALTER TABLE groups ADD COLUMN IF NOT EXISTS general_chat_topic_id INTEGER; ALTER TABLE groups ADD COLUMN IF NOT EXISTS important_info_topic_id INTEGER;"

```

**Важно:** Если вы используете новую установку, таблицы создаются автоматически при выполнении `scripts/first_setup.py`. Миграции нужны только для обновления существующей базы данных.

## 🔕 Управление уведомлениями

Для отключения автоматических уведомлений добавьте в файл `.env` следующие флаги:

```env
# Уведомления админам (создание/закрытие опросов, ошибки)
# Напоминание в 18:00 отметится в опросе (один раз)
ENABLE_ADMIN_NOTIFICATIONS=False

# Предупреждения курьерам о записи на слот (17:50)
ENABLE_COURIER_WARNINGS=False

# Уведомления о создании опросов в группы
ENABLE_POLL_CREATION_NOTIFICATIONS=False

# Уведомления админам о проблемах (health check каждые 30 минут)
ENABLE_HEALTH_CHECK_NOTIFICATIONS=False

# Верификация пользователей (по умолчанию отключена, установите True для включения)
ENABLE_VERIFICATION=True
```

**По умолчанию все уведомления включены** (`True`), **верификация отключена** (`False`).

⚠️ **Важно:** Изменения вступят в силу после перезапуска бота.

## 🧹 Управление сообщениями

Бот автоматически удаляет предыдущие системные сообщения (кроме опросов и результатов) для поддержания чистоты истории чата. Это помогает избежать засорения чата повторяющимися уведомлениями.

**Не удаляются:**
- Опросы
- Текстовые отчеты о результатах

## 📚 Дополнительная информация

### Обработка ночных опросов

Для ночных опросов (НОЧЬ-лево/НОЧЬ-право) результаты отображаются по опциям:

```
📊 Результаты опроса
Группа: НОЧЬ-лево
Дата: 21.12.2025

✅ Выхожу
   👥 Иван Иванов, Петр Петров
⏰ Помогаю до 00:00
   👥 Сидор Сидоров
🚫 Выходной
   👥 Мария Смирнова
```

### Источник данных для результатов

Результаты формируются из базы данных:
- **Имена пользователей:** Приоритет полного имени из таблицы `users` (first_name + last_name)
- **Слоты:** Данные из таблицы `poll_slots` с голосами из `user_votes`
- **Выходной:** Голоса с `slot_id = NULL` и `voted_option = "Выходной"`

**Важно:** Голоса сохраняются в БД при каждом голосовании благодаря явному `commit()` в `DatabaseMiddleware`, поэтому результаты всегда актуальны.

## 🔐 Безопасность

### Хранение секретов

**КРИТИЧЕСКИ ВАЖНО:** Никогда не коммитьте секреты (токены, пароли, ключи) в репозиторий!

#### Правила работы с секретами:

1. **Все секреты хранятся в `.env` файле**
   - Файл `.env` добавлен в `.gitignore` и не отслеживается git
   - Используйте `.env.example` как шаблон (без реальных значений)

2. **Pre-commit hook**
   - Автоматически проверяет наличие секретов перед коммитом
   - Если обнаружены секреты - коммит будет отклонен

3. **Если секрет был случайно закоммичен:**
   ```bash
   # 1. Немедленно смените все ключи!
   # 2. Используйте скрипт scripts/remove_secrets_from_history.sh
   # 3. Сделайте force push (только если уверены!)
   ```

#### Рекомендации по безопасности:

- Используйте надежные пароли (минимум 16 символов)
- Регулярно меняйте ключи, особенно после удаления из истории git
- Не храните секреты в коде, комментариях или логах
- Используйте GitHub Secrets для CI/CD (если используется)

Подробнее см. [docs/SECURITY.md](docs/SECURITY.md)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/bot.log`
2. Используйте команду `/logs` для просмотра последних логов
3. Используйте команду `/status` для проверки статуса системы
4. Проверьте настройки в `.env` файле

---

**Версия:** 1.0  
**Последнее обновление:** Декабрь 2025
