# Telegram Bot для планирования смен

Бот для автоматического создания и управления опросами о планировании смен в рабочих группах.

## ⚠️ Важные обновления

### ✅ Исправлена критическая проблема с сохранением голосов

**Проблема:** Ранее голоса не сохранялись в базу данных при голосовании из-за отсутствия явного `commit()` в `DatabaseMiddleware`, что приводило к:
- Пустым текстовым отчетам
- Некорректной работе проверки неотметившихся курьеров
- Некорректному обновлению счетчиков слотов
- Потере всех голосов при перезапуске бота

**Решение:** 
- ✅ Добавлен явный `commit()` в `DatabaseMiddleware` после успешного выполнения обработчиков
- ✅ Добавлен `rollback()` при ошибках для корректной обработки исключений
- ✅ Теперь при каждом голосовании:
  - Голос автоматически сохраняется в таблицу `user_votes`
  - Слот обновляется (`current_users++`, добавление `user_id` в `user_ids`)
  - Поддерживается смена выбора (удаление из старого слота, добавление в новый)
  - Корректно обрабатывается опция "Выходной"

**Результат:** Все голосования теперь корректно сохраняются в базу данных, текстовые отчеты и проверки курьеров работают правильно!

### 🔄 Улучшена надежность закрытия опросов

Добавлена многоуровневая система проверок:
- Основная задача в 19:00
- Периодические проверки каждые 5 минут с 19:05 до 19:55
- Финальная проверка в 20:00
- Проверка при старте бота (если запущен после 19:00)

Это гарантирует, что опросы будут закрыты даже при сбоях или перезапуске бота.

## 📋 Функционал

### Для админов
- **Настройка групп** (ЗИЗ-1...ЗИЗ-14, НОЧЬ-лево/право)
- **Конфигурация слотов времени**
- **Управление расписанием опросов**
- **Получение отчетов и статистики**
- **Мониторинг системы**

### Для пользователей
- **Верификация при первом использовании** (запрос имени и фамилии)
- **Участие в ежедневных опросах**
- **Просмотр своих голосов**
- **Получение напоминаний**

## 🚀 Быстрый старт

### 1. Установка

```bash
git clone <repository>
cd telegram-shift-bot
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

pip install -r requirements.txt
playwright install chromium
```

### 2. Настройка

```bash
cp .env.example .env
```

Отредактируйте `.env`:
- **BOT_TOKEN** — токен бота
- **ADMIN_IDS** — ID админов через запятую
- **Пароли и хосты БД/Redis**

### 3. Запуск базы данных

```bash
docker-compose up -d
```

### 4. Инициализация БД

```bash
python scripts/first_setup.py
```

### 5. Запуск бота

```bash
python src/main.py
```

### 6. Остановка бота

Бот корректно обрабатывает сигналы остановки и завершает работу с сохранением всех данных.

#### Остановка при запуске напрямую:

**Способ 1: Ctrl+C (рекомендуется)**
```bash
# Нажмите Ctrl+C в терминале, где запущен бот
^C
```

**Способ 2: Найти процесс и остановить**
```bash
# Найти процесс бота
ps aux | grep "python.*main.py"

# Остановить по PID (замените PID на номер процесса)
kill -SIGTERM <PID>

# Или принудительно (если не помогает)
kill -9 <PID>
```

#### Остановка при запуске в Docker:

```bash
# Остановка контейнера (если бот запущен в Docker)
docker stop <container_name>

# Или через docker-compose
docker-compose stop
```

#### Остановка при запуске через systemd:

```bash
# Остановка службы
sudo systemctl stop telegram-shift-bot

# Или с перезагрузкой
sudo systemctl restart telegram-shift-bot
```

**⚠️ Важно:**
- Бот корректно обрабатывает сигналы `SIGINT` (Ctrl+C) и `SIGTERM` (docker stop, systemd stop)
- При остановке бот автоматически:
  - Завершает текущие задачи
  - Закрывает соединения с базой данных
  - Останавливает планировщик задач
  - Закрывает браузер (если использовался для скриншотов)
  - Закрывает соединения с Redis
- Не используйте `kill -9` без крайней необходимости — это может привести к потере данных

## 📁 Структура проекта

```text
telegram-shift-bot/
├── src/
│   ├── handlers/
│   ├── models/
│   ├── services/
│   ├── middlewares/
│   ├── states/
│   ├── utils/
│   └── main.py
├── config/
├── db/
├── reports/
├── logs/
├── backups/
├── tests/
└── scripts/
```

## ⚙️ Команды бота

### 👤 Пользовательские команды

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом. Показывает приветственное сообщение и список доступных команд. При первом использовании запускает процесс верификации |
| `/help` | Показать подробную справку по всем командам с примерами использования |
| `/my_votes` | Просмотр своих голосов в опросах (в разработке) |
| `/schedule` | Просмотр расписания смен (в разработке) |

**Верификация:**
При первом использовании бота пользователь автоматически получает запрос на ввод имени и фамилии. Эти данные сохраняются и используются для отображения в результатах опросов.

### 👑 Админские команды

#### Админ-панель

| Команда | Описание |
|---------|----------|
| `/admin` | Открыть админ-панель с кнопками для быстрого доступа к функциям |

**Админ-панель включает:**
- ➕ **Создать группу для ЗИЗ** — пошаговое создание новой группы (название → chat_id) с уведомлением о необходимости настройки тем
- ⚙️ **Настройки слотов** — выбор группы и настройка слотов времени с подсказками формата ввода
- ⏰ **Настройка расписания** — просмотр текущих настроек и редактирование через пошаговый ввод:
  - Время создания опросов (hh:mm)
  - Время закрытия опросов (hh:mm)
  - Часы напоминаний (через запятую или `0` для отключения)
  - Изменения сохраняются в `.env` файл
- 🔍 **Проверить слоты** — проверка всех активных групп на незаполненные слоты и неотметившихся курьеров с автоматической отправкой замечаний
- 📌 **Установить тему** — автоматическая установка topic_id из контекста для 4 типов тем:
  - 📋 Отметки на слот (`telegram_topic_id`)
  - 📥 Приход/уход (`arrival_departure_topic_id`)
  - 💬 Общий чат (`general_chat_topic_id`)
  - 📢 Важная информация (`important_info_topic_id`)
  - Выполните выбор темы в нужной теме форум-группы для автоматического определения topic_id
- 📝 **Создать опросы вручную** — создание опросов для всех активных групп (для тестирования). Если опросы уже существуют, они будут отправлены первым сообщением в личку администратора
- 🔄 **Пересоздать опросы** — принудительное создание опросов с закрытием существующих активных опросов. Требует подтверждения, так как данные голосования будут потеряны
- 📊 **Вывести результат** — выбор группы и просмотр результатов опроса (скриншот или текстовый формат)
- 🔒 **Досрочно закрыть опрос** — выбор группы, закрытие опроса и отправка результатов (для тестирования)
- 🔎 **Найти и открыть опросы на завтра** — принудительный поиск опросов на завтра, синхронизация из Telegram (если опросы есть в группах, но отсутствуют в БД), создание скриншотов и текстовых отчетов с результатами. Результаты содержат фамилию и имя курьеров, выбранные слоты времени. Все результаты отправляются админу в личку
- 📸 **Ручная отправка скриншотов выхода** — загрузка скриншотов выхода для групп ЗИЗ-1 до ЗИЗ-14 по очереди. После получения всех скриншотов выбирается целевая группа, и все скриншоты отправляются в тему "отметка на слот" выбранной группы
- 📢 **Рассылка по группам** — отправка сообщения во все группы в выбранную тему (4 типа тем)

#### Управление группами

| Команда | Описание | Пример |
|---------|----------|--------|
| `/add_group` | Добавить новую группу в систему. Если выполнить команду в теме форум-группы, topic_id определится автоматически | `/add_group ЗИЗ-1 -1001234567890 [123]`<br>`123` - опционально, ID темы |
| `/setup_ziz` | Настроить слоты времени для группы. После команды введите слоты в формате `время_начала-время_конца:лимит` | `/setup_ziz ЗИЗ-1`<br>Затем: `07:30-19:30:3` |
| `/set_topic` | Установить topic_id для темы "отметки на слот". Если выполнить команду в теме, topic_id определится автоматически | `/set_topic ЗИЗ-1 [123]`<br>или `/set_topic ЗИЗ-1` (в теме) |
| `/set_arrival_topic` | Установить topic_id для темы "приход/уход" | `/set_arrival_topic ЗИЗ-1 123` |
| `/set_general_topic` | Установить topic_id для темы "общий чат" | `/set_general_topic ЗИЗ-1 123` |
| `/set_important_topic` | Установить topic_id для темы "важная информация" | `/set_important_topic ЗИЗ-1 123` |
| `/get_topic_id` | Показать topic_id из контекста текущего сообщения (выполните в нужной теме) | `/get_topic_id` |
| `/list_groups` | Показать список всех зарегистрированных групп с их статусом и настройками | `/list_groups` |

**Настройка тем:**
- `telegram_topic_id` — тема "отметки на слот" (где создаются опросы)
- `arrival_departure_topic_id` — тема "приход/уход" (куда отправляются результаты)
- `general_chat_topic_id` — тема "общий чат" (для напоминаний)
- `important_info_topic_id` — тема "важная информация" (для важных сообщений)

**💡 Рекомендация:** Используйте админ-панель (`/admin`) для установки тем — это проще и быстрее, так как topic_id определяется автоматически из контекста.

#### Опросы и отчеты

| Команда | Описание | Пример |
|---------|----------|--------|
| `/create_polls` | Создать опросы вручную для всех активных групп (для тестирования) | `/create_polls` |
| `/get_report` | Получить отчет по группе за указанную дату | `/get_report ЗИЗ-1 17.12.2025` |
| `/generate_all_reports` | Сгенерировать отчеты для всех групп (в разработке) | `/generate_all_reports` |


**⚠️ Важно:** У каждой группы ЗИЗ могут быть **индивидуальные настройки** времени слотов и количества людей на них. Например:
- **ЗИЗ-1** может иметь слоты: `07:30-19:30:3`, `08:00-20:00:2`
- **ЗИЗ-2** может иметь слоты: `10:00-22:00:1`, `12:00-24:00:4`
- **ЗИЗ-3** может иметь совершенно другие слоты

Каждая группа настраивается отдельно через команду `/setup_ziz` или через админ-панель.

**Формат ввода слотов:**

При настройке группы через `/setup_ziz` слоты вводятся в следующем формате:

```
время_начала-время_конца:лимит
```

**Примеры:**
- `07:30-19:30:3` - слот с 07:30 до 19:30, лимит 3 человека
- `08:00-20:00:2` - слот с 08:00 до 20:00, лимит 2 человека
- `10:00-22:00:1` - слот с 10:00 до 22:00, лимит 1 человек

Можно вводить несколько слотов сразу (каждый с новой строки) или по одному. Для завершения настройки отправьте `готово`.

**Формат отображения в опросах:**
Опции в опросах отображаются в следующем формате:
- Лимит 1: `10:00-22:00 - 1 человек`
- Лимит 2: `10:00-22:00 - 1 человек, 10:00-22:00 - 2 человека`
- Лимит 3+: `10:00-22:00 - 1 человек, 10:00-22:00 - 2 человека, - 3 человека`

**При создании опроса** бот автоматически использует слоты, настроенные для конкретной группы.

### ⏰ Автоматическое расписание

Бот автоматически выполняет следующие действия в течение 24 часов:

#### 09:00 — Создание опросов на следующий день
1. **Создание опросов** во всех активных группах в существующей теме "отметки на слот"
2. **Закрепление сообщений** с опросами (без уведомления)
3. **Создание слотов** в базе данных для каждого опроса
4. **Уведомления админам** о количестве созданных опросов и ошибках (если включено)

**Повторная попытка:** Если создание опроса не удалось, бот повторяет попытку через 5 минут для неудачных групп.

#### 17:50 — Предупреждения о записи на слот
Бот автоматически проверяет незаполненные слоты и отправляет предупреждения курьерам:
- **Незаполненные слоты** — слоты, где `current_users < max_users`
- **Неотметившиеся курьеры** — курьеры, которые не проголосовали в опросе (идентифицируются по тегам 8958, 7368, 6028 или верификации)

Предупреждения отправляются в тему "общий чат" **другой группы** (не той, где опрос) с тэгами курьеров и информацией о проблемах со слотами.

**Условие:** Выполняется только если `ENABLE_COURIER_WARNINGS=True` в `.env`.

#### 18:00 — Напоминание о закрытии опроса
- Отправляет напоминание "Остался один час до конца опроса!" в тему "общий чат"
- Для всех активных групп с опросом на завтра

**Условие:** Выполняется только если `ENABLE_GROUP_REMINDERS=True` в `.env`.

#### 19:00 — Закрытие опросов (основная задача)
1. **Автоматическое закрытие** всех активных опросов текущего дня через Telegram API
2. **Обновление статуса** в БД на "closed"
3. **Создание скриншота** результатов опроса через Playwright (или текстовый отчет при ошибке)
4. **Отправка результатов** в тему "приход/уход" с подписанными именами и фамилиями пользователей
5. **Отправка замечаний** курьерам, которые не отметились

**Условие:** Закрывает только опросы, где текущее время >= времени закрытия группы (`poll_close_time`).

#### 19:05, 19:10, 19:15, ..., 19:55 — Периодические проверки закрытия
- Повторяет логику закрытия опросов **каждые 5 минут**
- Гарантирует закрытие опросов, даже если основная задача была пропущена или бот был перезапущен

#### 20:00 — Финальная проверка закрытия
- Последняя попытка закрыть опросы на случай, если что-то было пропущено

#### Каждые 30 минут (XX:30) — Health Check
- Проверяет наличие опросов на завтра для всех групп
- Проверяет статус опросов (должны быть "active")
- Проверяет доступность бота в группах
- Отправляет уведомления админам о проблемах

**Условие:** Выполняется только если `ENABLE_HEALTH_CHECK_NOTIFICATIONS=True` в `.env`.

#### При старте бота — Проверка просроченных опросов
- Если бот запущен после 19:00, проверяет и закрывает просроченные опросы
- Выполняется через 5 секунд после старта бота
- Гарантирует, что опросы будут закрыты даже после перезапуска бота

**Настройка времени:**
Время можно настроить двумя способами:

1. **Через админ-панель** (рекомендуется):
   - Откройте `/admin` → "⏰ Настройка расписания" → "✏️ Редактировать"
   - Пошагово введите время создания опросов и закрытия опросов
   - Изменения автоматически сохраняются в `.env` файл

2. **Вручную в файле `.env`**:
   - `POLL_CREATION_HOUR` и `POLL_CREATION_MINUTE` (по умолчанию 9:00)
   - `POLL_CLOSING_HOUR` и `POLL_CLOSING_MINUTE` (по умолчанию 19:00)

⚠️ **Важно:** Изменения вступят в силу после перезапуска бота.

**Надежность закрытия опросов:**
Бот использует многоуровневую систему проверок для гарантии закрытия опросов:
- Основная задача в 19:00
- Периодические проверки каждые 5 минут с 19:05 до 19:55
- Финальная проверка в 20:00
- Проверка при старте бота (если запущен после 19:00)

Это гарантирует, что опросы будут закрыты даже при сбоях или перезапуске бота.

### 🔕 Управление уведомлениями (контроль спама)

Для отключения автоматических уведомлений добавьте в файл `.env` следующие флаги:

```bash
# Уведомления админам (создание/закрытие опросов, ошибки)
ENABLE_ADMIN_NOTIFICATIONS=False

# Предупреждения курьерам о записи на слот (17:50)
ENABLE_COURIER_WARNINGS=False

# Уведомления о создании опросов в группы
ENABLE_POLL_CREATION_NOTIFICATIONS=False

# Уведомления админам о проблемах (health check каждые 30 минут)
ENABLE_HEALTH_CHECK_NOTIFICATIONS=False

# Верификация пользователей (по умолчанию отключена, установите True для включения)
ENABLE_VERIFICATION=True
```

**По умолчанию все уведомления включены** (`True`), **верификация отключена** (`False`). Установите `False` для отключения конкретного типа уведомлений, `True` для включения верификации.

**Верификация:** По умолчанию верификация отключена (`ENABLE_VERIFICATION=False`), пользователи могут писать в чат и участвовать в опросах без прохождения процесса верификации. Установите `ENABLE_VERIFICATION=True` для включения верификации.

⚠️ **Важно:** Изменения вступят в силу после перезапуска бота.

### 📊 Статистика и мониторинг

| Команда | Описание | Пример |
|---------|----------|--------|
| `/stats` | Показать статистику системы: количество групп, опросов, голосов | `/stats` |
| `/status` | Показать статус системы: использование памяти, CPU, диска | `/status` |
| `/logs` | Показать последние 20 строк логов системы | `/logs` |

### 📝 Формат слотов времени

### 📌 Работа с темами (Topics) в форум-группах

Бот поддерживает работу с темами форум-групп Telegram для организации опросов и уведомлений.

#### Типы тем

1. **"отметки на слот"** (`telegram_topic_id`)
   - Сюда создаются опросы каждый день в 09:00
   - Опросы создаются в существующей теме (тема не создается автоматически)

2. **"приход/уход"** (`arrival_departure_topic_id`)
   - Сюда отправляются скриншоты результатов опросов
   - Результаты содержат подписанные имена и фамилии пользователей

3. **"общий чат"** (`general_chat_topic_id`)
   - Сюда отправляются напоминания о закрытии записи
   - Ежечасные напоминания с 14:00 до 18:00
   - Финальное напоминание в 18:30

4. **"важная информация"** (`important_info_topic_id`)
   - Сюда можно отправлять важные информационные сообщения
   - Используется для рассылки через админ-панель

#### Как установить topic_id

**Через админ-панель (рекомендуется):**
1. Откройте `/admin` → "📌 Установить тему"
2. Выберите тип темы (Отметки на слот, Приход/уход, Общий чат, Важная информация)
3. **Важно:** Выполните выбор темы прямо в нужной теме форум-группы
4. Выберите группу из списка
5. Topic_id определится автоматически и будет установлен

**Через команды:**
1. **Автоматически** — выполните команду `/get_topic_id` в нужной теме форум-группы
2. **При создании группы** — выполните `/add_group название chat_id` прямо в теме, topic_id определится автоматически
3. **При установке темы** — выполните `/set_topic название_группы` в теме, topic_id определится автоматически
4. **Вручную** — перешлите сообщение из темы боту @RawDataBot, он покажет `message_thread_id`

#### Примеры настройки

```bash
# 1. Узнать topic_id темы "отметки на слот"
# Перейдите в тему и выполните:
/get_topic_id
# Бот покажет: Topic ID из контекста: 123

# 2. Установить topic_id для группы
/set_topic ЗИЗ-1 123
# Или проще - выполните команду прямо в теме:
/set_topic ЗИЗ-1
# Бот автоматически определит topic_id

# 3. Настроить тему "приход/уход" через админ-панель
# /admin → Установить тему → Приход/уход (выполните в нужной теме)
# Или через команду:
/set_arrival_topic ЗИЗ-1 123

# 4. Настроить тему "общий чат" через админ-панель
# /admin → Установить тему → Общий чат (выполните в нужной теме)
# Или через команду:
/set_general_topic ЗИЗ-1 123

# 5. Настроить тему "важная информация" через админ-панель
# /admin → Установить тему → Важная информация (выполните в нужной теме)
# Или через команду:
/set_important_topic ЗИЗ-1 123
```

**Важно:** После установки topic_id все опросы будут создаваться в указанной теме. опрос  в "отметки на слот" создается автоматически каждый день в 09:00.

## 🔐 Верификация пользователей

При первом использовании бота пользователь автоматически проходит процесс верификации:

1. **Запрос имени** — бот просит ввести имя (2-50 символов, только буквы)
2. **Запрос фамилии** — бот просит ввести фамилию (2-50 символов, только буквы)
3. **Сохранение данных** — имя и фамилия сохраняются в базе данных
4. **Верификация завершена** — пользователь может участвовать в опросах

**Важно:** 
- Верификация обязательна для участия в опросах
- Имя и фамилия используются для отображения в результатах опросов
- Данные хранятся в таблице `users` в базе данных

## 📊 Результаты опросов

### Формирование результатов

При закрытии опроса (19:00) бот автоматически:

1. **Создает скриншот результатов:**
   - Открывает опрос в Telegram Web через Playwright
   - Делает скриншот результатов опроса
   - Добавляет подпись с названием группы и датой
   - Добавляет имена пользователей из базы данных (полные имена с фамилиями)
   - Сохраняет в `reports/{group_name}/{date}.png`

2. **Если скриншот не создается:**
   - Создает текстовый отчет из базы данных
   - Показывает все слоты с именами пользователей
   - Показывает список выбравших "Выходной"
   - Сохраняет в `reports/{group_name}/{date}.txt`

3. **Отправка результатов:**
   - Скриншот или текстовый отчет отправляется в тему "приход/уход"
   - Если тема не найдена, отправка не выполняется (ошибка логируется)

### Формат текстового отчета

```
📊 Результаты опроса
Группа: ЗИЗ-1
Дата: 21.12.2025

⏰ 07:30-19:30
   👥 Иван Иванов, Петр Петров
⏰ 08:00-20:00
   👥 Сидор Сидоров
⏰ 10:00-22:30
   👥 Нет записей

🚫 Выходной
   👥 Мария Смирнова
```

### Обработка ночных опросов

Для ночных опросов результаты отображаются по опциям:

```
📊 Результаты опроса
Группа: НОЧЬ-лево
Дата: 21.12.2025

✅ Выхожу
   👥 Иван Иванов, Петр Петров
⏰ Помогаю до 00:00
   👥 Сидор Сидоров
🚫 Выходной
   👥 Мария Смирнова
```

### Источник данных

Результаты формируются из базы данных:
- **Имена пользователей:** Приоритет полного имени из таблицы `users` (first_name + last_name)
- **Слоты:** Данные из таблицы `poll_slots` с голосами из `user_votes`
- **Выходной:** Голоса с `slot_id = NULL` и `voted_option = "Выходной"`

**Важно:** Голоса сохраняются в БД при каждом голосовании благодаря явному `commit()` в `DatabaseMiddleware`, поэтому результаты всегда актуальны.

### Тестирование результатов

- **Через админ-панель:** `/admin` → "📊 Вывести результат" → выберите группу
- **Досрочное закрытие:** `/admin` → "🔒 Досрочно закрыть опрос" → выберите группу

## 🔧 Технологии

- **Python 3.11+**
- **Aiogram 3.x**
- **PostgreSQL** — хранение данных о группах, опросах, пользователях
- **Redis** — FSM storage для состояний бота
- **Playwright** — создание скриншотов результатов опросов
- **Docker** — контейнеризация PostgreSQL и Redis
- **APScheduler** — планирование автоматических задач

## 📝 Миграции базы данных

После обновления кода необходимо применить миграции:

```bash
# 1. Создание таблицы users (для верификации пользователей)
docker exec shift-bot-postgres psql -U bot_user -d shift_bot -c "CREATE TABLE IF NOT EXISTS users (id BIGINT PRIMARY KEY, first_name VARCHAR(100), last_name VARCHAR(100), username VARCHAR(100), is_verified BOOLEAN DEFAULT FALSE, created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_users_verified ON users(is_verified);"

# 2. Добавление колонок для тем в таблицу groups
docker exec shift-bot-postgres psql -U bot_user -d shift_bot -c "CREATE TABLE IF NOT EXISTS users (id BIGINT PRIMARY KEY, first_name VARCHAR(100), last_name VARCHAR(100), username VARCHAR(100), is_verified BOOLEAN DEFAULT FALSE, created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_users_verified ON users(is_verified);"

# 3. Создание таблицы screenshot_checks (для записи времени скриншотов)
docker exec shift-bot-postgres psql -U bot_user -d shift_bot -c "CREATE TABLE IF NOT EXISTS screenshot_checks (id SERIAL PRIMARY KEY, group_id INTEGER NOT NULL UNIQUE REFERENCES groups(id) ON DELETE CASCADE, last_screenshot_time TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP, created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP); CREATE INDEX IF NOT EXISTS idx_screenshot_checks_group_id ON screenshot_checks(group_id);"

# Или использовать готовые SQL-скрипты (если они скопированы в контейнер):
docker exec shift-bot-postgres psql -U bot_user -d shift_bot -f /docker-entrypoint-initdb.d/create_users_table.sql
docker exec shift-bot-postgres psql -U bot_user -d shift_bot -f /docker-entrypoint-initdb.d/add_topic_columns.sql
docker exec shift-bot-postgres psql -U bot_user -d shift_bot -f /docker-entrypoint-initdb.d/add_important_info_topic_id.sql
```

**Важно:** Если вы используете новую установку, таблицы создаются автоматически при выполнении `scripts/first_setup.py`. Миграции нужны только для обновления существующей базы данных.

## 📢 Рассылка по группам

Админы могут отправлять сообщения во все активные группы через админ-панель:

1. Откройте `/admin` → "📢 Рассылка по группам"
2. Выберите тип темы для рассылки:
   - 📋 Отметки на слот
   - 📥 Приход/уход
   - 💬 Общий чат
   - 📢 Важная информация
3. Введите сообщение для рассылки
4. Сообщение будет отправлено во все активные группы в выбранную тему

**Примечание:** Рассылка работает только для групп, у которых настроена соответствующая тема.

## 🧹 Управление сообщениями

Бот автоматически удаляет предыдущие системные сообщения (кроме опросов и результатов) для поддержания чистоты истории чата. Это помогает избежать засорения чата повторяющимися уведомлениями.

**Не удаляются:**
- Опросы
- Скриншоты результатов
- Текстовые отчеты о результатах

## 🧪 Тестирование

Проект включает комплексные unit-тесты для проверки функциональности бота.

### Запуск тестов

```bash
# Запуск всех тестов
pytest

# Запуск с подробным выводом
pytest -v

# Запуск конкретного файла тестов
pytest tests/test_user_handlers.py

# Запуск конкретного теста
pytest tests/test_user_handlers.py::test_cmd_help_user

# Запуск с покрытием кода
pytest --cov=src --cov-report=html
```

### Структура тестов

Тесты организованы по модулям:

- **`test_user_handlers.py`** - тесты пользовательских команд (`/start`, `/help`, `/my_votes`, `/schedule`)
- **`test_verification_handlers.py`** - тесты процесса верификации пользователей
- **`test_admin_handlers.py`** - тесты админских команд (`/stats`, `/create_polls`)
- **`test_admin_panel.py`** - тесты админ-панели (создание групп, настройка слотов, расписания, тем, опросов, рассылки)
- **`test_poll_handlers.py`** - тесты обработки ответов на опросы
- **`test_monitoring_handlers.py`** - тесты команд мониторинга (`/status`, `/logs`)
- **`test_report_handlers.py`** - тесты команд отчетов (`/get_report`, `/generate_all_reports`)
- **`test_slot_validation.py`** - тесты валидации формата слотов времени
- **`test_group_service.py`** - тесты сервиса групп
- **`test_poll_service.py`** - тесты сервиса опросов
- **`test_user_service.py`** - тесты сервиса пользователей

### Покрытие функционала

Тесты покрывают следующие аспекты:

#### Пользовательские функции
- ✅ Верификация при первом использовании
- ✅ Команды `/start`, `/help`, `/my_votes`, `/schedule`
- ✅ Участие в опросах (обработка ответов)

#### Админские функции
- ✅ Админ-панель (`/admin`)
- ✅ Создание групп через админ-панель
- ✅ Настройка слотов времени
- ✅ Настройка автоматического расписания
- ✅ Установка тем форум-групп
- ✅ Создание и пересоздание опросов
- ✅ Вывод результатов опросов
- ✅ Досрочное закрытие опросов
- ✅ Рассылка по группам

#### Валидация
- ✅ Формат слотов времени (`время_начала-время_конца:лимит`)
- ✅ Валидация времени создания/закрытия опросов
- ✅ Валидация часов напоминаний

#### Мониторинг и отчеты
- ✅ Статистика системы (`/stats`)
- ✅ Статус системы (`/status`)
- ✅ Просмотр логов (`/logs`)
- ✅ Генерация отчетов (`/get_report`)

### Зависимости для тестирования

Все необходимые зависимости для тестирования указаны в `requirements.txt`:
- `pytest` - фреймворк для тестирования
- `pytest-asyncio` - поддержка асинхронных тестов
- `pytest-cov` - покрытие кода (опционально)

### Фикстуры

В `tests/conftest.py` определены общие фикстуры:
- `mock_bot` - мок бота
- `mock_session` - мок сессии БД
- `mock_poll_repo` - мок репозитория опросов
- `mock_group_repo` - мок репозитория групп
- `mock_user_repo` - мок репозитория пользователей
- `mock_screenshot_service` - мок сервиса скриншотов

## 🔍 Идентификация курьеров по тегам

Бот автоматически идентифицирует курьеров ДС 8958 по тегам в их именах Telegram:
- **8958** - основной тег курьеров ДС 8958
- **7368** - дополнительный тег
- **6028** - дополнительный тег

### Как это работает

1. **Автоматическая идентификация**: При проверке участников группы бот ищет теги в display names пользователей Telegram
2. **Автоматическое добавление в БД**: Курьеры с тегами автоматически добавляются в базу данных при первом голосовании
3. **Извлечение имени**: Имя и фамилия извлекаются из display name (удаляются цифры и теги)

### Примеры идентификации

- `"Иванов Иван 8958"` → Имя: "Иванов", Фамилия: "Иван"
- `"Петров Петр 7368"` → Имя: "Петров", Фамилия: "Петр"
- `"Сидоров Сидор 6028"` → Имя: "Сидоров", Фамилия: "Сидор"

### Скрипты для работы с курьерами

```bash
# Добавить курьеров из опросов в БД (автоматически по тегам)
python scripts/add_couriers_from_list.py

# Сравнить список сотрудников с БД
python scripts/compare_employees_with_db.py [путь_к_файлу.xml]

# Показать всех пользователей в БД
python scripts/list_all_users.py

# Обновить имя пользователя
python scripts/update_user_name.py

# Проверить статус опросов во всех группах
python scripts/check_polls_status.py

# Проверить проблемы со слотами (незаполненные, превышение лимита)
python scripts/check_slots_issues.py

# Получить список участников группы через Telegram API
python scripts/get_all_group_members.py <chat_id>
```

### Скрипты для синхронизации опросов

```bash
# Синхронизировать опросы из Telegram в БД (на завтра)
# Ищет опросы в группах и сохраняет их в БД, если их там нет
python scripts/sync_polls_from_telegram.py

# Синхронизировать опросы на конкретную дату
python scripts/sync_polls_from_telegram.py --date 2025-12-25
```

**Примечание:** Скрипт синхронизации пытается найти опросы в закрепленных сообщениях групп. Для форум-групп с темами поиск может быть ограничен из-за ограничений Telegram Bot API. Если опросы не найдены автоматически, используйте функцию "🔎 Найти и открыть опросы на завтра" в админ-панели, которая проверяет опросы в БД и создает для них скриншоты и отчеты.

### Как работает идентификация курьеров

1. **При проверке слотов** бот автоматически:
   - Проверяет всех верифицированных пользователей из БД
   - Ищет курьеров в голосах опросов по тегам (8958, 7368, 6028)
   - Автоматически добавляет найденных курьеров в БД
   - Извлекает имя и фамилию из display name Telegram

2. **Теги в именах**: Курьеры идентифицируются по наличию тегов в их display names:
   - `"Иванов Иван 8958"` → автоматически распознается как курьер ДС 8958
   - `"Петров Петр 7368"` → автоматически распознается как курьер
   - `"Сидоров Сидор 6028"` → автоматически распознается как курьер

3. **Автоматическое добавление**: При первом голосовании курьер с тегом автоматически добавляется в БД с извлеченным именем и фамилией

## 📝 История изменений

### Последние обновления

#### Исправления
- ✅ **Исправлена критическая проблема с сохранением голосов**: Добавлен явный `commit()` в `DatabaseMiddleware` для корректного сохранения всех голосований в базу данных
- ✅ **Исправлена ошибка обработки ответов на опросы**: Обработчик `poll_answer` теперь использует dependency injection вместо позиционных аргументов
- ✅ **Исправлена ошибка TypeError в админ-панели**: Параметр `data` в функциях `callback_show_results_for_group` и `callback_close_poll_for_group` теперь опциональный
- ✅ **Исправлены ADMIN_IDS**: Удалены ID ботов из списка администраторов для предотвращения ошибок "bots can't send messages to bots"
- ✅ **Улучшена идентификация курьеров**: Бот автоматически идентифицирует курьеров по тегам (8958, 7368, 6028) в именах Telegram
- ✅ **Автоматическое добавление курьеров**: Курьеры с тегами автоматически добавляются в БД при первом голосовании
- ✅ **Удалена кнопка "Группы со словом 'в'"**: Удалена неиспользуемая кнопка из админ-панели

#### Новые функции
- ✅ **Скрипт проверки опросов**: `scripts/check_polls_status.py` - проверяет наличие опросов во всех группах на сегодня и завтра
- ✅ **Скрипт проверки слотов**: `scripts/check_slots_issues.py` - выявляет незаполненные слоты (никто не отметился) и превышения лимита. Исключает случаи, когда все слоты заполнены
- ✅ **Скрипт добавления курьеров из опросов**: `scripts/add_couriers_from_list.py` - добавляет курьеров из опросов в БД по тегам
- ✅ **Скрипт добавления курьеров из скриншотов**: `scripts/add_couriers_from_screenshots.py` - добавляет курьеров ДС 8958 из скриншотов Telegram групп
- ✅ **Скрипт сравнения сотрудников**: `scripts/compare_employees_with_db.py` - сравнивает список сотрудников (XML/текст) с БД
- ✅ **Скрипт управления группами**: 
  - `scripts/delete_test_group.py` - удаление групп
  - `scripts/remove_test_from_group_names.py` - переименование групп
- ✅ **Скрипт управления пользователями**:
  - `scripts/list_all_users.py` - показать всех пользователей в БД
  - `scripts/update_user_name.py` - обновить имя пользователя
- ✅ **Скрипт получения участников группы**: `scripts/get_all_group_members.py` - получить список участников через Telegram API
- ✅ **Скрипт вывода результатов опросов**: `scripts/show_polls_results.py` - выводит результаты всех активных опросов на вчера, сегодня и завтра с информацией о проголосовавших
- ✅ **Скрипт проверки голосов**: `scripts/check_votes_by_poll_id.py` - проверяет голоса по telegram_poll_id из логов
- ✅ **Скрипт проверки ADMIN_IDS**: `scripts/check_admin_ids.py` - проверяет, какие ID в ADMIN_IDS являются ботами
- ✅ **Скрипт исправления ADMIN_IDS**: `scripts/fix_admin_ids.py` - автоматически удаляет ID ботов из ADMIN_IDS

#### Улучшения
- ✅ **Идентификация по тегам**: Бот автоматически идентифицирует курьеров по тегам (8958, 7368, 6028) в именах Telegram даже если они не прошли верификацию
- ✅ **Автоматическое добавление курьеров**: Курьеры с тегами автоматически добавляются в БД при первом голосовании
- ✅ **Улучшенная проверка слотов**: При проверке слотов учитываются курьеры, идентифицированные по тегам. Проверяются опросы на сегодня и завтра
- ✅ **Автоматическая отправка замечаний**: Бот автоматически проверяет незаполненные слоты и неотметившихся курьеров в 17:50, отправляя замечания с тэгами курьеров в группы
- ✅ **Кнопка проверки слотов в админ-панели**: Добавлена кнопка "🔍 Проверить слоты" для ручной проверки всех групп и отправки замечаний курьерам в любое время
- ✅ **Автоматическое извлечение имен**: Имена и фамилии автоматически извлекаются из display names Telegram (удаляются теги и цифры)
- ✅ **Проверка незаполненных слотов**: Бот выявляет слоты, на которые никто не отметился (current_users = 0)
- ✅ **Проверка превышения лимита**: Бот выявляет слоты с превышением лимита (current_users > max_users)
- ✅ **Исключение заполненных групп**: Если все слоты заполнены корректно, замечания не отправляются
- ✅ **Разнообразные шаблоны замечаний**: Бот использует различные варианты текстов замечаний для более естественного общения (см. `docs/WARNING_EXAMPLES.md`)

