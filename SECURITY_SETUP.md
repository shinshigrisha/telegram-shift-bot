# 🔐 Настройка безопасности - Полная инструкция

## ✅ Что уже сделано:

1. ✅ **Pre-commit hook установлен** - автоматически проверяет наличие секретов перед каждым коммитом
2. ✅ **Скрипт проверки создан** (`scripts/check_secrets.sh`) - проверяет staged файлы на наличие секретов
3. ✅ **Скрипт удаления секретов создан** (`scripts/remove_secrets_from_history.sh`) - для удаления секретов из истории git
4. ✅ **Скрипт подготовки создан** (`scripts/prepare_secrets_removal.sh`) - подготовка к удалению секретов
5. ✅ **Обновлен .env.example** - шаблон без реальных ключей
6. ✅ **Добавлена документация** (`docs/SECURITY.md`) - подробные инструкции по безопасности
7. ✅ **Обновлен README.md** - добавлен раздел о безопасности
8. ✅ **git-filter-repo установлен** (если требуется)

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Секреты в истории Git!

В истории репозитория обнаружены реальные ключи:
- `BOT_TOKEN`
- `DB_PASSWORD`
- `REDIS_PASSWORD`
- `ENCRYPTION_KEY`

**Эти ключи нужно немедленно сменить и удалить из истории Git!**

## 🚨 Что нужно сделать СРОЧНО:

### Шаг 1: Сменить все ключи

**КРИТИЧЕСКИ ВАЖНО:** Сначала смените все ключи, иначе удаление из истории не поможет!

#### 1.1. BOT_TOKEN

```bash
# 1. Откройте @BotFather в Telegram
# 2. Отправьте /revoke и выберите вашего бота
# 3. Получите новый токен командой /token
# 4. Обновите BOT_TOKEN в .env файле
```

#### 1.2. DB_PASSWORD

```bash
# Подключитесь к PostgreSQL
docker exec -it shift-bot-postgres psql -U bot_user -d shift_bot

# Измените пароль
ALTER USER bot_user WITH PASSWORD 'новый_надежный_пароль';

# Выход
\q

# Обновите DB_PASSWORD в .env файле
# Перезапустите контейнер PostgreSQL
docker-compose restart postgres
```

#### 1.3. REDIS_PASSWORD

```bash
# 1. Отредактируйте docker-compose.yml или создайте redis.conf
# 2. Измените requirepass на новый пароль
# 3. Перезапустите Redis: docker-compose restart redis
# 4. Обновите REDIS_PASSWORD в .env файле
```

Или через Docker:

```bash
# Остановите Redis
docker stop shift-bot-redis

# Запустите с новым паролем
docker run -d --name shift-bot-redis \
  -p 6379:6379 \
  redis:7-alpine \
  redis-server --requirepass новый_пароль

# Обновите REDIS_PASSWORD в .env файле
```

#### 1.4. ENCRYPTION_KEY

```bash
# Сгенерируйте новый ключ
python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# Обновите ENCRYPTION_KEY в .env файле
```

### Шаг 2: Удалить секреты из истории Git

⚠️ **ВНИМАНИЕ:** Это изменит историю репозитория!

#### Вариант 1: Использовать готовый скрипт (рекомендуется)

```bash
# Запустите скрипт удаления
bash scripts/remove_secrets_from_history.sh
```

Скрипт:
- Заменит реальные ключи на `<REMOVED>` в истории
- Потребует подтверждения
- Автоматически установит git-filter-repo если нужно

#### Вариант 2: Вручную

Подробные инструкции см. в `scripts/README_SECURITY.md`

### Шаг 3: Force push

⚠️ **ВНИМАНИЕ:** Это перезапишет историю на удаленном репозитории!

```bash
# После удаления секретов из истории
git push --force --all
git push --force --tags
```

**Важно:** Убедитесь, что вы сменили все ключи перед force push!

### Шаг 4: Уведомить коллабораторов

Если у вас есть коллабораторы, они должны:

1. Удалить старый репозиторий локально
2. Клонировать заново: `git clone <repository-url>`
3. Создать новый `.env` файл с новыми ключами

## 🔍 Проверка после выполнения

### Проверка 1: .env не отслеживается Git

```bash
# Должно быть пусто (ничего не выводится)
git ls-files | grep -E "\.env$"
```

Если выводится `.env`, удалите его из индекса:

```bash
git rm --cached .env
git commit -m "Remove .env from tracking"
```

### Проверка 2: Секреты удалены из истории

```bash
# Должно быть пусто или только <REMOVED>
git log --all --source --full-history -p | grep -E "BOT_TOKEN|DB_PASSWORD|REDIS_PASSWORD|ENCRYPTION_KEY" | grep -v "<REMOVED>"
```

Если выводятся реальные ключи, повторите шаг 2.

### Проверка 3: Pre-commit hook работает

```bash
# Попробуйте добавить секрет в файл
echo "BOT_TOKEN=test123" >> test.txt
git add test.txt

# Pre-commit hook должен заблокировать коммит
git commit -m "Test"

# Удалите тестовый файл
rm test.txt
```

### Проверка 4: Скрипт проверки работает

```bash
# Запустите скрипт проверки
bash scripts/check_secrets.sh
```

## 📋 Чек-лист безопасности

- [ ] Все ключи сменены (BOT_TOKEN, DB_PASSWORD, REDIS_PASSWORD, ENCRYPTION_KEY)
- [ ] .env файл не отслеживается Git
- [ ] Секреты удалены из истории Git
- [ ] Выполнен force push
- [ ] Коллабораторы уведомлены (если есть)
- [ ] Pre-commit hook работает
- [ ] Скрипт проверки работает
- [ ] Проверка истории не показывает реальные ключи

## 📚 Дополнительная информация

### Документация

- **Подробные инструкции:** `scripts/README_SECURITY.md`
- **Документация по безопасности:** `docs/SECURITY.md`
- **Раздел безопасности в README:** `README.md` (раздел "🔐 Безопасность")

### Скрипты

- `scripts/check_secrets.sh` - проверка staged файлов на секреты
- `scripts/remove_secrets_from_history.sh` - удаление секретов из истории
- `scripts/prepare_secrets_removal.sh` - подготовка к удалению секретов

### Рекомендации по безопасности

1. **Используйте надежные пароли** (минимум 16 символов, буквы, цифры, символы)
2. **Регулярно меняйте ключи**, особенно после удаления из истории git
3. **Не храните секреты** в коде, комментариях или логах
4. **Используйте GitHub Secrets** для CI/CD (если используется)
5. **Проверяйте логи** на наличие случайно залогированных секретов
6. **Используйте разные пароли** для разных окружений (dev, staging, production)

## ⚠️ Важные напоминания

- **Никогда не коммитьте .env файл** - он в `.gitignore`
- **Всегда проверяйте** staged файлы перед коммитом (pre-commit hook делает это автоматически)
- **Если секрет был закоммичен** - немедленно смените его и удалите из истории
- **После force push** все коллабораторы должны переклонировать репозиторий

---

**Безопасность - это не разовое действие, а постоянный процесс! 🔒**
