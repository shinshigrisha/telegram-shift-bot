# 🔐 Настройка безопасности - Инструкция

## ✅ Что было сделано:

1. **Создан pre-commit hook** - автоматически проверяет наличие секретов перед каждым коммитом
2. **Создан скрипт проверки** (`scripts/check_secrets.sh`) - проверяет staged файлы на наличие секретов
3. **Создан скрипт удаления секретов** (`scripts/remove_secrets_from_history.sh`) - для удаления секретов из истории git
4. **Обновлен .env.example** - шаблон без реальных ключей
5. **Добавлена документация** (`docs/SECURITY.md`) - подробные инструкции по безопасности
6. **Обновлен README.md** - добавлен раздел о безопасности

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Секреты в истории Git!

В истории репозитория обнаружены реальные ключи:
- `BOT_TOKEN=<REMOVED>`
- `DB_PASSWORD=<REMOVED>`
- `REDIS_PASSWORD=<REMOVED>`
- `ENCRYPTION_KEY=<REMOVED>=`

## 🚨 Что нужно сделать СРОЧНО:

### 1. Сменить все ключи немедленно!

См. подробные инструкции в `scripts/README_SECURITY.md`

### 2. Удалить секреты из истории Git

```bash
# Вариант 1: Использовать готовый скрипт
bash scripts/remove_secrets_from_history.sh

# Вариант 2: Вручную (см. scripts/README_SECURITY.md)
```

### 3. Force push (только после смены ключей!)

```bash
git push --force --all
git push --force --tags
```

## 📋 Проверка безопасности

После выполнения всех шагов проверьте:

```bash
# Проверить, что .env не отслеживается
git ls-files | grep -E "\.env$"

# Проверить историю на наличие секретов
git log --all --source --full-history -p | grep -E "BOT_TOKEN|DB_PASSWORD|REDIS_PASSWORD|ENCRYPTION_KEY" | grep -v "<REMOVED>"
```

## 📚 Дополнительная информация

- Подробные инструкции: `scripts/README_SECURITY.md`
- Документация по безопасности: `docs/SECURITY.md`
- Раздел безопасности в README: `README.md` (раздел "🔐 Безопасность")
